{
  "name": "my-little-quest",
  "version": "0.1.0",
  "private": true,
  "description": "Gamified certification learning game — certification-agnostic engine",
  "scripts": {
    "dev:local": "cd local && npm run dev",
    "dev:online": "cd online && npm run dev",
    "build:local": "cd local && npm run build",
    "build:online": "cd online && npm run build",
    "validate": "node scripts/validate-content.js",
    "stats": "node scripts/stats.js",
    "new-cert": "node scripts/new-certification.js",
    "precommit": "npm run validate",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
